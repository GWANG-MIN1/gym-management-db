[alembic]
script_location = migrations
prepend_sys_path = .

# sqlalchemy.url 은 여기에 두지 않는다.
# 접속 정보는 api/database.py 한 곳에서만 관리하고(로컬은 DATABASE_URL,
# EC2 는 Secrets Manager), migrations/env.py 가 그 엔진을 그대로 사용한다.
